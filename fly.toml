# fly.toml — איתמר Slack bot
# Socket Mode only — no inbound connections needed, no public IP required.
app = 'standing-togther'
primary_region = 'fra'

[build]

[[restart]]
  policy = 'always'

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
