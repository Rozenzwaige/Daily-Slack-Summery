name: Daily News Summary

on:
  schedule:
    # 09:00 Israel Daylight Time (IDT = UTC+3)  → 06:00 UTC  (summer: Mar–Oct)
    # 09:00 Israel Standard Time (IST = UTC+2)  → 07:00 UTC  (winter: Oct–Mar)
    # Running at 09:00 to ensure delivery by 10:00 (buffer for server load).
    - cron: "0 6 * * *"

  # Allow triggering manually from the GitHub Actions UI (for testing)
  workflow_dispatch:

jobs:
  send-news-summary:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: news_summary/requirements.txt

      - name: Install dependencies
        run: pip install -r news_summary/requirements.txt

      - name: Run daily news summary
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: python news_summary/main.py
